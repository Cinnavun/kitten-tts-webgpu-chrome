{
  "name": "webgpu",
  "version": "1.0.0",
  "description": "Kitten TTS WebGPU Extension",
  "main": "background.js",
  "scripts": {
    "build": "esbuild src/sidepanel.js src/offscreen.js --bundle --format=esm --outdir=dist",
    "watch": "esbuild src/sidepanel.js src/offscreen.js --bundle --format=esm --outdir=dist --watch"
  },
  "dependencies": {
    "kitten-tts-webgpu": "^0.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}